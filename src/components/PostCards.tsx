import PostCard from "./AdjacentPostCard";

export default async function PostCards({ path }: { path: string }) {
  return (
    <div className="w-full h-44 flex">
      {/* <PostCard info={test[0]} />
      <PostCard info={test[1]} /> */}
    </div>
  );
}
